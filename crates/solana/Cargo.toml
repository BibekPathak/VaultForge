[package]
name = "vaultforge-solana"
version = "1.0.0"
edition = "2021"

[dependencies]
vaultforge-crypto = { path = "../crypto" }
serde = { version = "1", features = ["derive"] }
log = "0.4"
thiserror = "1"
zeroize = "1"
