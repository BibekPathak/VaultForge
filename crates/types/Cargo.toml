[package]
name = "vaultforge-types"
version = "1.0.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = "0.8"
zeroize = "1"
uuid = "1.0"
time = "0.3"
