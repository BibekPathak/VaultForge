apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vaultforge.fullname" . }}-config
  labels:
    {{- include "vaultforge.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
