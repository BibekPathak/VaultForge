apiVersion: v1
kind: Service
metadata:
  name: {{ include "vaultforge.fullname" . }}
  labels:
    {{- include "vaultforge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "vaultforge.selectorLabels" . | nindent 4 }}
